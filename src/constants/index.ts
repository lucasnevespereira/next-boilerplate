export const DEFAULT_THEME = "dark";

export enum THEME {
  LIGHT = "light",
  DARK = "dark",
  CUPCAKE = "cupcake",
  CUSTOM_THEME = "custom-theme",
}

export enum NOTIFICATION {
    INFO = "info",
    SUCCESS = "success",
    WARNING = "warning",
    ERROR = "error",
}